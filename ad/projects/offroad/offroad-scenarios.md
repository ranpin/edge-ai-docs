# 越野与非结构化场景

*越野自动驾驶的地形理解、可通行性分析与数据稀缺破局 —— 结构化道路之外的另一套感知范式*

> [!TIP]
> **本篇讲什么**
>
> 这是自动驾驶板块「项目/专项层」的一篇，聚焦**越野与非结构化场景**——没有车道线、没有交通标志、没有明确道路边界的自然环境（沙地、泥地、碎石、草地、水坑、树根、灌木）。它与 [BEV 感知与端到端](../../general/perception.html) 讲的结构化道路感知是**两套不同的范式**：结构化道路是「检测已知目标」，越野是「理解地形物理属性」，因此单独成篇。
>
> 本篇覆盖：越野场景概述与应用、地形分类与可通行性分析、结构化道路 vs 越野的核心差异、数据稀缺与前沿方向、端侧落地考量。
>
> 与 [Alpamayo-Edge（8B VLM Orin 量化实战）](https://ranpin.github.io/qwen-trajectory-prediction/) 并列，本篇是**专项场景深潜**——侧重领域方法与前沿方向，不绑定具体量产实现，因此不含实测性能数字。

> [!NOTE]
> **数据口径**
>
> 本篇为纯领域通识/专项方法，与全站口径一致：**只讲方法，不给「标准答案」式性能数字**。文中出现的可通行性评分、推荐速度、比例等均为**示例参数**，仅用于演示推导方法本身，不代表实测；数据集名称（RUGD/RELLIS-3D 等）、传感器物理特性属公开事实。请代入你自己的平台与场景数据计算。

## 1. 概述与应用场景

越野自动驾驶 (Off-road Autonomous Driving) 面对的是与结构化道路完全不同的环境：**没有车道线、没有交通标志、没有明确的道路边界**，取而代之的是复杂多变的自然地形——沙地、泥地、碎石、草地、水坑、树根、灌木等。传统结构化道路感知算法几乎完全失效。其核心转变是：从"**检测已知目标**"变为"**理解地形物理属性**"(从外观推断含水量、承载力等)。

典型应用：军事无人车辆、农业自动化、矿山运输、灾难救援、星球探测 (火星/月球巡视器)。

> [!NOTE]
> **为什么越野是「另一套范式」而非「更难的城区」**
>
> 城区自动驾驶的难点在长尾目标与博弈交互，但底层假设成立：有地图先验、有车道结构、目标类别有限可枚举。越野把这三条假设全部打破——**无地图先验**（可通行区域要实时推断）、**无车道结构**（路径由地形物理属性决定）、**开放集障碍**（岩石/倒木/动物不可枚举）。因此越野不是「把城区模型训得更鲁棒」，而是要换一套以「地形物理属性推理」为核心的感知-规划栈。

## 2. 地形分类与可通行性分析

**地形分类**判断每个像素/区域属于什么地形，类别比结构化道路语义分割更细粒度、边界更模糊：

| 大类 | 子类 | 可通行性 | 感知难点 |
| :--- | :--- | :--- | :--- |
| **硬质地面** | 干燥沥青、砾石路、岩石面 | 高通行性 | 与未铺装道路的边界模糊 |
| **松软地面** | 干沙、湿沙、淤泥、松土 | 中/低 (依赖含水量) | 视觉外观相似但物理性质差异巨大 |
| **植被覆盖** | 短草、高草、灌木、落叶堆 | 短草可通行，高草/灌木可能不可 | 高度和密度难以从单帧图像判断 |
| **水体** | 浅水坑、深水区、泥水混合 | 浅水可通行，深水危险 | 水深无法从视觉直接判断，反射干扰 |
| **不可通行** | 悬崖、深沟、大型岩石、倒木 | 不可通行 | 几何障碍需要 3D 感知 |

**可通行性分析 (Traversability Analysis)** 综合几何信息 (LiDAR/双目生成高度图，计算坡度/粗糙度/阶跃高度) 与语义信息 (地形类型 → 摩擦系数/陷入风险)：

```python
# 几何可通行性分析伪代码
def geometric_traversability(height_map, vehicle_params):
    slope = compute_slope(height_map)          # 坡度 (度)
    roughness = compute_roughness(height_map)   # 粗糙度 (高度方差)
    step = compute_step_height(height_map)      # 阶跃高度 (m)

    score = 1.0                                  # 可通行性评分 (0-1, 1 为完全可通行)
    score -= clip(slope / vehicle_params.max_slope, 0, 1) * 0.4
    score -= clip(roughness / vehicle_params.max_roughness, 0, 1) * 0.3
    score -= clip(step / vehicle_params.max_step, 0, 1) * 0.3
    return clip(score, 0, 1)
```

语义可通行性把地形类型映射为评分与推荐速度（下表为**示例参数**，演示「地形→评分→速度」的映射方法，实际需按车辆动力学与实测标定）：干燥砾石路 ~0.9 / 30-50 km/h，干沙 ~0.5 / 10-20 km/h，湿泥 ~0.2 / 5-10 km/h，短草 ~0.7 / 15-30 km/h，浅水坑 (<20cm) ~0.4 / 5-15 km/h，岩石面 ~0.6 (颠簸) / 10-20 km/h。

> [!TIP]
> **几何 + 语义双通道，缺一不可**
>
> 纯几何可通行性（坡度/粗糙度/阶跃）能挡住悬崖、深沟、大岩石，但分不清「硬砾石」和「软泥坑」——两者几何上可能都平整。纯语义可通行性（地形分类→摩擦/陷入风险）能区分材质，但漏掉几何障碍。越野必须**双通道融合**：几何通道保证「不撞不翻」，语义通道保证「不陷不滑」，最终评分取两者的保守组合（min 或加权）。

## 3. 结构化道路 vs 越野：核心差异

| 维度 | 结构化道路 | 越野/非结构化 |
| :--- | :--- | :--- |
| **道路边界** | 车道线、路沿、护栏，边界清晰 | 无明确边界，可通行区域需推断 |
| **交通规则** | 交通法规、红绿灯、标志牌 | 无交通规则，纯物理约束 |
| **目标类别** | 车辆、行人、自行车等有限类别 | 岩石、树木、动物、坑洞等开放类别 |
| **感知核心** | 检测已知类别的目标 | 理解地形物理属性 |
| **规划逻辑** | 遵循车道、保持车距、让行 | 选择可通行路径、避开风险区域 |
| **失败模式** | 碰撞、违规 | 陷车、侧翻、悬挂损坏 |
| **数据可用性** | 海量 (nuScenes, Waymo, KITTI) | 极度稀缺 (RUGD, RELLIS-3D) |

## 4. 数据稀缺与前沿方向

越野数据集 (RUGD、RELLIS-3D) 规模比结构化道路小 1-2 个数量级，且场景覆盖不全。缓解路径：

- **合成数据**：用 Unreal Engine 5 / Unity 程序化生成地形 (随机高度图 + 材质 + 光照)，核心挑战是 **Sim-to-Real Gap**。
- **域适应**：DANN 对抗特征对齐、CycleGAN/UNIT 风格迁移、自训练伪标签、MoCo/SimCLR 自监督预训练。
- **少样本学习**：Prototypical Network / MAML，仅用 5-10 张标注图识别罕见地形 (沼泽、冰面)。

前沿方向：

- **自监督可通行性**：用行驶中的 IMU 振动、车轮滑移率、悬挂行程回溯标注相机帧，无需人工标注持续积累数据。
- **多模态融合**：越野中单一传感器严重不可靠 (摄像头受尘土遮挡、LiDAR 被草地干扰)，必须 Camera+LiDAR+Radar+IMU 融合；**Radar** 对穿透烟尘、大雨有独特优势，价值远高于结构化道路 (呼应 [BEV 感知与端到端](../../general/perception.html) §4 的 4D 成像雷达)。
- **基础模型迁移**：用 SAM、DINOv2 做 zero-shot 分割/特征提取，再用少量越野数据微调分类头；DINOv2 特征即使不微调，在越野地形分类上也表现优异。

## 5. 端侧落地考量

越野往往在**远离基础设施、通信受限、平台算力/功耗受限**的环境下作业（矿山、农田、灾区、星球），这让端侧落地比城区更苛刻。几条工程原则（方法层面，不含实测数字）：

- **离线自主是默认假设**：不能依赖 V2X/云端回传（呼应 [驾驶 Agent 与安全](../../general/agent-safety.html) §3 的断网降级）。可通行性估计、路径规划、障碍规避必须全部在车端闭环完成。
- **传感器退化是常态而非异常**：尘土糊相机、泥浆盖 LiDAR、雨水扰雷达。端侧要有**传感器健康自诊断 + 降级策略**——某一模态退化时，可通行性估计自动切换到剩余可靠模态，而不是直接失效。
- **可通行性要实时、增量**：高度图/语义图随车辆行进滚动更新（类似 [端侧部署](../../general/deployment.html) §1.3 的时序特征缓存思路），而非每帧全量重算；几何通道（坡度/粗糙度）算力轻可高频，语义通道（地形分类）算力重可低频，两者分频融合。
- **Radar 的端侧价值被低估**：在烟尘/大雨/暗光下，Radar 是少数仍可靠的模态，且算力开销远低于 LiDAR 点云处理。越野端侧传感器配置应给 4D 成像雷达更高权重。
- **物理属性推理对模型设计的要求**：「从外观推断含水量/承载力」本质是**回归物理量**而非分类语义标签，这对端侧模型的输出头设计、不确定性估计（陷车风险要给出置信度）提出要求——不确定时宁可保守绕行。

> [!NOTE]
> **越野需要根本不同的感知范式**
>
> 结构化道路感知是"检测已知目标"(分类 + 定位)；越野感知是"理解地形属性"(从外观推断物理性质)。这种**从语义分类到物理属性推理**的转变，是越野感知的核心挑战，也是它无法直接复用城区 BEV/端到端栈的根本原因。
